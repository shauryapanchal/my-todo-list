import Head from "next/head";

const HeadEx = () => {
  return (
    <Head>
      <title>Todo List Sync</title>
      <meta name="description" content="Web application for synchronizing tasks between todo list applications" />
      <meta name="viewport" content="width=device-width, initial-scale=1" />
      <link rel="icon" href="/favicon.ico" />
    </Head>
  )
}

export default HeadEx;